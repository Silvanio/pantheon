package com.pantheon.service.entity;

/** The decision state of a single {@link PurchaseRequestApproval} step. */
public enum PurchaseRequestApprovalStatus {
    PENDING,
    APPROVED,
    REJECTED
}
